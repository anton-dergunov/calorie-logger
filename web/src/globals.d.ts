/**
 * The build's version identity, substituted by Vite; see `define` in `vite.config.ts`.
 * `__APP_BUILD__` is a UTC minute stamp and is what update checks compare.
 */
declare const __APP_VERSION__: string;
declare const __APP_BUILD__: string;
